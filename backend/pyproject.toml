[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "susurro-backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "edge-tts",
    "httpx",
    "lingua-language-detector",
    "pydantic>=2",
]

[project.optional-dependencies]
dev = [
    "pytest",
]

[tool.hatch.build.targets.wheel]
packages = ["src/susurro_backend"]

[tool.pytest.ini_options]
testpaths = ["src"]
addopts = "-m 'not slow'"
markers = [
    "slow: marks tests as slow (network calls). Run with: pytest -m slow",
]
